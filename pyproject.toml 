[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "dynamic-llm-selector"
version = "0.1.0"
description = "Dynamic LLM Model Selector based on Prompt Complexity"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
"tiktoken>=0.5.0",
"anthropic>=0.20.0",
"openai>=1.0.0",
"transformers>=4.35.0",
"pydantic>=2.0.0",
]
[project.optional-dependencies]
dev = [
"pytest>=7.0.0",
"mypy>=1.0.0",
"black>=23.0.0",
]
[tool.setuptools.packages.find]
where = ["src"]